import { View, Text } from 'react-native';

const CoursesPage = () => {
    return (
        <View
            style={{ justifyContent: 'center', alignItems: 'center', flex: 1 }}>
            <Text>Tab [Courses]</Text>
        </View>
    );
};
export default CoursesPage;
